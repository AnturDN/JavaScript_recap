let name = 'Antur'// we can change the value (if we want) of the variable if we use "let"

const name2 = "Shuvro"// we cannot change the value if we use const

if(name.length > 4){
    name = 'Joy'
    name2 = "Nobel"
}

console.log(name2);
