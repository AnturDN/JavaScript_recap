
//----------1-------------

var friendAge = [10, 20, 30, 40, 50]
// If I set friendAge.indexOf(200), than the output will be -1, beacuse if the value is not avaible in the array it will show the output -1;

var position = friendAge.indexOf(20)

var anturAge = friendAge[1]

friendAge[2] = 100;

console.log(friendAge[2]);
